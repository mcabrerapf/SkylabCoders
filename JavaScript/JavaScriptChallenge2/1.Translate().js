//translate()
function translate(word){
	var wordTr = "";
	wordTr = word;
	wordTrans = [];
	for (i=0; i<word.length; i++){
		if ("aeiou".indexOf(wordTr) = -1){
			wordTrans.push(word.tr[i])
			console.log(wordTrans)
		}
	}
}
